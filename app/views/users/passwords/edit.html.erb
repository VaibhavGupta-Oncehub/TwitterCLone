<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta content="width=device-width, initial-scale=1.0" name="viewport">

</head>

<body>
  <section id="breadcrumbs" class="breadcrumbs">
    <div class="container">
      <h2 class="text-center"> Change Your Password<span>🔑 </span></h2>
    </div>
  </section><!-- End Breadcrumbs -->

  <section class="inner-page">
    <div class="container h-100">
      <div class="row d-flex justify-content-center align-items-center h-100">
        <div class="col-lg-12 col-xl-11">
          <div class="card text-black" style="border-radius: 25px;">
            <div class="card-body p-md-5">

              <div class="row justify-content-center">
                <div class="col-md-10 col-lg-6 col-xl-5 order-2 order-lg-1">

                  <p class="text-center h2 fw-bold mb-5 mx-1 mx-md-4 mt-4">Create a New Password</p>
                  <p>Change your password in three easy steps. This will help you to secure your password!</p>
	                <ol class="list-unstyled">
	                    <li><span class="text-primary text-medium">1. </span>Always use a longer password.</li>
	                    <li><span class="text-primary text-medium">2. </span>Make sure your password is alphanumeric.</li>
	                    <li><span class="text-primary text-medium">3. </span>Never Share your password with others.</li>
	                </ol>
                  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
                    <%= render "devise/shared/error_messages", resource: resource %>
                    <%= f.hidden_field :reset_password_token %>
                    <%# Password Field %>
                    <div class="d-flex flex-row align-items-center mb-4">
                      <i class="fas fa-lock fa-lg me-3 fa-fw"></i>
                      <div class="form-outline flex-fill mb-0">
                        <%= f.label :password,"New Password",class:"form-label" %><br />
                        <% if @minimum_password_length %>
                          <label class="form-label">(<%= @minimum_password_length %> characters minimum)</label>
                        <% end %>
                        <br/>
                        <%= f.password_field :password, autocomplete: "new-password", class:"form-control" %>
                      </div>
                    </div>

                    <%# Confirm your password %>
                    <div class="d-flex flex-row align-items-center mb-4">
                      <i class="fas fa-key fa-lg me-3 fa-fw"></i>
                      <div class="form-outline flex-fill mb-0">
                        <%= f.label :password_confirmation,"Confirm new password", class:"form-label" %><br />
                        <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"form-control"%>
                      </div>
                    </div>

                    <%# Sumbit button %>
                    <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-4">
                      <%= f.submit "Change password" ,class:"btn btn-primary btn-lg"%>
                    </div>

                  <% end %>
                  <div class="divider d-flex align-items-center my-4">
                    <p class="text-center fw-bold mx-3 mb-0 text-muted">OR</p>
                  </div>
                
                </div>

                <div class="col-md-10 col-lg-6 col-xl-7 text-right d-flex align-items-center order-1 order-lg-2">
                  <img src="https://www.officepuzzle.com/wp-content/uploads/2017/01/reset-password.jpg" class="img-fluid" alt="Sample image">
                </div>

              </div>
              <div class="container justify-content-center">
                <div class="btn-group btn-group-lg" role="group" aria-label="Basic example">
                 <%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
                    <%= link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name), class:"btn btn-primary btn-lg btn-block d-flex justify-content-center m-2 "  %><br />
                  <% end %>
                  <%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
                    <%= link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name), class:"btn btn-primary btn-lg btn-block d-flex justify-content-center m-2 "  %><br />
                  <% end %>
                </div>
              </div>

            </div>
          </div>
        </div>

      </div>
      
    </div>
  </section>

</body>

</html>                